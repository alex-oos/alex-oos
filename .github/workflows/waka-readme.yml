name: WakaTime Readme

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 20 * * *'

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-oos/waka-readme@main
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          BLOCKS: "⬜🟨🟩"
          TIME_RANGE: last_14_days
          SHOW_TIME: true
          SHOW_MASKED_TIME: true
          GH_TOKEN:  ${{ secrets.GH_TOKEN }}
